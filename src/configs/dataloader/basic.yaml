train:
  _target_: torch.utils.data.DataLoader
  batch_size: 12
  num_workers: 0
  pin_memory: True

inference:
  _target_: torch.utils.data.DataLoader
  batch_size: 1
  num_workers: 0
  pin_memory: True
